# Configuration reference

Configuration for the api-server (api.yaml) and agent (agent.yaml): koanf keys, env overrides (OTHERIX_ prefix, __ nesting), and defaults.

!!! warning "Work in progress"
    This page is a stub. The content is being written - see the
    [Architecture](../architecture.md) overview in the meantime.

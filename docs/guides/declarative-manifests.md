# Declarative manifests

Apply and remove resources from multi-document YAML manifests with `otherix create -f` / `delete -f`, and round-trip live resources with `get -o yaml`.

!!! warning "Work in progress"
    This page is a stub. The content is being written - see the
    [Architecture](../architecture.md) overview in the meantime.

name: ci

on:
  push:
    branches: ["**"]
  pull_request:

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26.4"
          cache: true
      - uses: golangci/golangci-lint-action@v9
        with:
          version: v2.12.1
          args: --timeout 5m

  vuln:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26.4"
          cache: true
      - name: govulncheck
        run: make vuln

  test-unit:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26.4"
          cache: true
      - name: Unit tests
        run: go test ./... -race -tags=test_fast_argon -coverprofile=coverage.out
      - uses: actions/upload-artifact@v7
        with:
          name: coverage
          path: coverage.out

  test-etcd:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26.4"
          cache: true
      # etcd-backed store + api e2e suites. Both embed etcd in-process, so no
      # Docker / Postgres service is needed.
      - name: Integration tests (etcd store + api e2e)
        run: make test-etcd

  test-netfabric:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26.4"
          cache: true
      # The agent data-plane netns suite (real bridge / tap / vxlan / nft
      # masquerade / wireguard over netlink) is //go:build linux,integration
      # and self-skips without CAP_NET_ADMIN. A GitHub-hosted ubuntu runner
      # gives us passwordless sudo (effective root) so it runs for real;
      # OTHERIX_NETFABRIC_REQUIRE=1 (set by the make target) turns any
      # capability-missing skip into a hard failure so a data-plane
      # regression cannot pass green by skipping.
      - name: Load data-plane kernel modules
        run: sudo modprobe wireguard vxlan nft_masq nft_chain_nat || true
      - name: Data-plane netns integration tests (root, skips fail)
        run: make test-netfabric-native

  build:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        binary: [api, agent]
        arch: [amd64, arm64]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26.4"
          cache: true
      - name: Build ${{ matrix.binary }} for linux/${{ matrix.arch }}
        env:
          GOOS: linux
          GOARCH: ${{ matrix.arch }}
          CGO_ENABLED: "0"
        run: |
          mkdir -p out
          go build -trimpath \
            -ldflags "-s -w -X github.com/otherix/otherix/internal/version.Version=${GITHUB_SHA::7}" \
            -o out/otherix-${{ matrix.binary }}-linux-${{ matrix.arch }} \
            ./cmd/${{ matrix.binary }}
      - uses: actions/upload-artifact@v7
        with:
          name: otherix-${{ matrix.binary }}-linux-${{ matrix.arch }}
          path: out/otherix-${{ matrix.binary }}-linux-${{ matrix.arch }}

  api-validate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "20"
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26.4"
          cache: true
      - name: Lint OpenAPI specs
        run: |
          for f in api/openapi/*.yaml; do
            case "$f" in *.bundled.yaml|*/oapi-codegen.yaml) continue ;; esac
            echo "::group::Linting $f"
            npx --yes @redocly/cli@2.31.2 lint "$f"
            echo "::endgroup::"
          done
      - name: Verify generated agent API
        run: make agent-api-verify

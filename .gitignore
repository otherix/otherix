# Build artifacts
/bin/
/dist/
*.test

# Test artifacts
/coverage.out
/coverage.html

# IDE
.idea/
.vscode/
*.swp

# Claude Code — personal/local files
.claude/
CLAUDE.local.md

# Local env
.env
.env.local

# OS
.DS_Store

# Docker volumes
/.docker-data/

# Local dev runtime state (PID files, logs from make local-dev-start)
/.local/

# OpenAPI bundles (generated by `make api-bundle`)
api/openapi/*.bundled.yaml

# Local overrides for the API preview compose stack
tools/api-preview/.env

# Go vendor directory
vendor/

# Git worktrees (used by subagent-driven workflows)
.worktrees/

# Per-environment local config overrides (never commit secrets)
*.local.yaml
deploy/config/*.local.yaml

# Bin output names
/bin/otherix-*

